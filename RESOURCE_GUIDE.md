# 如何添加美工资源 - 快速指南

### 第1步：准备图片文件

准备以下PNG图片（**必需**，缺少任何文件游戏将无法启动）：
- `player.png` - 玩家图片（60x60像素）
- `bullet.png` - 子弹图片（15x15像素）
- `background_main.png` - 主菜单背景（800x600像素）
- `background_game.png` - 游戏背景（800x600像素）
- `enemy.png` - 敌人图片（40x40像素）

### 第2步：放置文件

将图片文件放到以下位置：
```
our_game/
├── src/              ← 源代码
├── assets/           ← 在这里放图片！
│   ├── player.png
│   ├── bullet.png
│   ├── background_main.png
│   ├── background_game.png
│   └── enemy.png
├── CMakeLists.txt
└── README.md
```

### 第3步：编译和运行游戏

```bash
# 在 our_game 目录下创建 build 文件夹
mkdir build
cd build

# 配置项目
cmake ..

# 编译
cmake --build .

# 运行（资源会自动复制到 build/assets/）
./game_final.exe
```

## 📝 项目结构说明

```
our_game/                    ← Git 仓库根目录
├── assets/                  ← 资源文件夹（会被复制到 build/assets/）
│   ├── README.md           ← 资源说明文档
│   ├── player.png
│   ├── bullet.png
│   ├── background_main.png
│   ├── background_game.png
│   └── enemy.png
├── src/                     ← 源代码
│   ├── core/
│   ├── entities/
│   ├── ui/
│   └── ...
├── build/                   ← 编译输出（不提交到Git）
│   ├── game_final.exe
│   └── assets/             ← 自动复制的资源
├── CMakeLists.txt
├── README.md
└── RESOURCE_GUIDE.md       ← 本文档
```

### 添加新资源：

1. 将新的PNG文件放到 `our_game/assets/` 文件夹
2. 提交到Git：`git add assets/` 和 `git commit`
3. 推送到远程：`git push`
4. 队友拉取后重新编译即可使用

## 📝 示例文件获取

### 方法1：使用在线工具快速制作

1. 访问 https://www.pixilart.com/ 或 https://www.piskelapp.com/
2. 创建简单的像素图
3. 导出为PNG
4. 重命名并放到 `our_game/assets/` 文件夹

### 方法2：使用Paint快速制作

1. 打开Windows画图工具
2. 创建新图片，设置尺寸（如60x60）
3. 画一个简单的角色
4. 保存为PNG格式
5. 放到 `our_game/assets/` 文件夹

### 方法3：下载免费素材

推荐网站：
- https://opengameart.org/
- https://kenney.nl/assets
- https://itch.io/game-assets/free