#ifndef CONSTANTS_H
#define CONSTANTS_H

//部分边界判断需要，在此暂存，设计主窗口的时候请提供接口来获取这些值
inline int room_bound_x = 800;
inline int room_bound_y = 600;

inline int scene_bound_x = 800;
inline int scene_bound_y = 600;

#endif // CONSTANTS_H
