# 团队协作指南

## 🎯 项目结构

```
our_game/                    ← Git 仓库（唯一需要的文件夹）
├── assets/                  ← 游戏资源文件夹
│   ├── README.md
│   ├── background_main.png  (800×600)
│   ├── background_game.png  (800×600)
│   ├── player.png          (60×60)
│   ├── bullet.png          (15×15)
│   └── enemy.png           (40×40)
├── src/                     ← 源代码
│   ├── core/
│   ├── entities/
│   ├── ui/
│   └── ...
├── build/                   ← 构建输出（不提交到Git）
│   ├── game_final.exe
│   └── assets/             ← 自动复制的资源
├── CMakeLists.txt
├── README.md 
├── RESOURCE_GUIDE.md
├── COLLABORATION.md 
└── .gitignore
```

## 🚀 新成员快速上手

### 1. 首次克隆仓库

```bash
# 克隆远程仓库
git clone <your-repo-url>
cd our_game

# 查看文件结构
ls
# 应该看到: assets/, src/, CMakeLists.txt, README.md 等
```

### 2. 编译项目

```bash
# 创建构建目录
mkdir build
cd build

# 配置项目（Windows MinGW示例）
cmake ..

# 如果Qt未自动检测到，指定Qt路径：
# cmake -DCMAKE_PREFIX_PATH="C:/Qt/6.x.x/mingw_64" ..

# 编译
cmake --build .

# 完成！资源会自动复制到 build/assets/
```

### 3. 运行游戏

```bash
# Windows
./game_final.exe

# Linux/Mac
./game_final
```

## 📝 日常开发流程

### 开始工作前

```bash
# 1. 拉取最新代码
git pull

# 2. 重新编译（如果有更新）
cd build
cmake --build .
```

### 添加或修改代码

```bash
# 1. 修改文件（使用你喜欢的编辑器）
# 例如: 修改 src/entities/player.cpp

# 2. 重新编译测试
cd build
cmake --build .

# 3. 运行游戏测试
./game_final.exe
```

### 添加或修改美术资源

```bash
# 1. 将新的PNG文件放到 our_game/assets/ 文件夹
# 例如: assets/player.png

# 2. 提交到Git
git add assets/player.png
git commit -m "更新玩家图片"

# 3. 推送到远程
git push

# 4. 重新编译（资源会自动复制）
cd build
cmake --build .
```

### 提交你的修改

```bash
# 1. 查看修改的文件
git status

# 2. 添加修改的文件
git add src/entities/player.cpp
# 或添加所有修改: git add .

# 3. 提交
git commit -m "添加玩家跳跃功能"

# 4. 推送到远程
git push
```

## 🎨 美术资源管理

### 资源文件位置

**所有PNG图片都放在**: `our_game/assets/`

### 必需的资源文件

| 文件名                | 尺寸    | 说明       |
| --------------------- | ------- | ---------- |
| `background_main.png` | 800×600 | 主菜单背景 |
| `background_game.png` | 800×600 | 游戏背景   |
| `player.png`          | 60×60   | 玩家       |
| `bullet.png`          | 15×15   | 子弹       |
| `enemy.png`           | 40×40   | 敌人       |

### 更新资源流程

```bash
# 1. 美工提供新图片（例如 player.png）
# 2. 将图片复制到 our_game/assets/player.png
# 3. 提交到Git
git add assets/player.png
git commit -m "更新玩家外观"
git push

# 4. 其他成员拉取
git pull
cd build
cmake --build .  # 重新编译，资源自动复制
```

## 🔧 常见问题

### Q: 队友看不到我的代码/资源？

**A:** 确保你已经提交并推送：
```bash
git add .
git commit -m "描述修改"
git push
```

### Q: 拉取代码后编译失败？

**A:** 尝试清理并重新构建：
```bash
# 删除build文件夹
rm -rf build  # Linux/Mac
Remove-Item -Recurse -Force build  # Windows PowerShell

# 重新创建并编译
mkdir build
cd build
cmake ..
cmake --build .
```

### Q: 游戏提示资源加载失败？

**A:** 检查以下几点：
1. `our_game/assets/` 文件夹是否存在
2. 所有必需的PNG文件是否都在
3. 是否重新编译了项目 (`cmake --build .`)
4. 检查 `build/assets/` 文件夹是否有文件

### Q: 修改了资源但游戏中看不到变化？

**A:** 需要重新编译：
```bash
cd build
cmake --build .
```

### Q: Git冲突怎么办？

**A:** 如果拉取时有冲突：
```bash
# 1. 先提交你的修改
git add .
git commit -m "保存我的修改"

# 2. 拉取并合并
git pull

# 3. 如果有冲突，手动解决冲突文件
# 4. 解决后重新提交
git add .
git commit -m "解决冲突"
git push
```

## 📂 什么需要提交到Git？

### ✅ 需要提交

- `src/` - 所有源代码
- `assets/` - 所有PNG资源文件
- `CMakeLists.txt` - 构建配置
- `*.md` - 文档文件
- `.gitignore` - Git忽略配置

### ❌ 不要提交

- `build/` - 编译输出（已在.gitignore中）
- `*.exe` - 可执行文件
- `*.o`, `*.obj` - 编译中间文件
- IDE配置文件（如 `.vscode/`, `.idea/`）

## 🌿 分支管理建议

### 主要分支

- `main` - 稳定版本，只合并测试通过的代码
- `dev` - 开发分支，日常开发在这里进行

### 功能分支

开发新功能时创建分支：

```bash
# 1. 从dev创建新分支
git checkout dev
git pull
git checkout -b feature/player-jump

# 2. 开发功能...

# 3. 提交
git add .
git commit -m "完成玩家跳跃功能"
git push -u origin feature/player-jump

# 4. 在GitHub上创建Pull Request合并到dev
```

## 💡 最佳实践

1. **频繁拉取**: 每天开始工作前 `git pull`
2. **及时提交**: 完成一个小功能就提交，不要积累太多修改
3. **清晰注释**: Commit message要清楚描述做了什么
4. **测试后推送**: 确保代码能编译通过再 `git push`
5. **沟通协调**: 修改公共文件前在群里说一声

## 📞 获取帮助

- **项目说明**: `README.md`
- **资源指南**: `RESOURCE_GUIDE.md`
- **UI文档**: `UI_GUIDE.md`
- **资源详情**: `assets/README.md`

## 🎓 Git基础命令速查

```bash
# 查看状态
git status

# 拉取最新代码
git pull

# 添加文件
git add <文件名>
git add .  # 添加所有修改

# 提交
git commit -m "提交说明"

# 推送
git push

# 查看历史
git log

# 查看分支
git branch

# 切换分支
git checkout <分支名>

# 创建并切换分支
git checkout -b <新分支名>
```
